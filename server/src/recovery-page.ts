import{renderTopNav,TOP_NAV_CSS}from"./nav";const css=`${TOP_NAV_CSS}*{box-sizing:border-box}body{margin:0;background:#f4f5f2;font-family:system-ui;color:#172019}.page{max-width:520px;margin:48px auto;padding:16px}.card{background:#fff;border:1px solid #dfe4df;border-radius:16px;padding:26px}label{display:block;font-weight:700;font-size:12px;margin-top:14px}input{width:100%;padding:11px;margin-top:5px;border:1px solid #cdd5ce;border-radius:8px}button{width:100%;min-height:44px;margin-top:18px;border:0;border-radius:8px;background:#193f2b;color:#fff;font-weight:800}.status{margin-top:12px;word-break:break-word}.dev{padding:10px;background:#fff4cf;border-radius:8px}`;
export function renderRecoveryRequestPage(){return page("Recover account",`<p>Enter your email. The response will not reveal whether an account exists.</p><form id="form"><label>Email<input name="email" type="email" required></label><button>Send recovery link</button><div class="status" role="status"></div></form>`,`const f=document.getElementById('form');f.onsubmit=async e=>{e.preventDefault();const b=f.querySelector('button'),s=f.querySelector('.status');b.disabled=true;b.textContent='Sending…';const r=await fetch('/api/account/recovery/request',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.email.value})});const j=await r.json();s.innerHTML='If the account exists, a recovery link has been prepared.'+(j.developmentUrl?'<div class="dev"><a href="'+j.developmentUrl+'">Open development recovery link</a></div>':'');b.textContent='Link requested'}`);}
export function renderTokenPage(mode:"verify"|"reset",token:string){const reset=mode==="reset";return page(reset?"Choose a new password":"Verify your email",`<form id="form">${reset?'<label>New password<input name="password" type="password" minlength="12" required></label>':''}<button>${reset?'Reset password':'Verify email'}</button><div class="status" role="status"></div></form>`,`const f=document.getElementById('form');f.onsubmit=async e=>{e.preventDefault();const b=f.querySelector('button'),s=f.querySelector('.status');b.disabled=true;b.textContent='Working…';const r=await fetch('/api/account/${reset?'recovery/reset':'verify'}',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token:${JSON.stringify(token)}${reset?',password:f.password.value':''}})});const j=await r.json();s.textContent=r.ok?'${reset?'Password reset. Sign in again.':'Email verified.'}':j.error||'This link is invalid or expired.';b.textContent=r.ok?'Done':'Try again';b.disabled=r.ok}`);}
function page(title:string,body:string,script:string){return`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${title} — Dende</title><style>${css}</style></head><body>${renderTopNav("account")}<main class="page"><section class="card"><h1>${title}</h1>${body}</section></main><script>${script}</script></body></html>`;}
